<% provide(:title, @artist.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @artist %>
        <%= @artist.name %>
      </h1>
    </section>
  </aside>
</div>