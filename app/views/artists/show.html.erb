<% provide(:title, @artist.name) %>
<div class="container">
  <div class="rou">
    <div class="col-md-4">
      <div class="artist-info">
        <h2><%= @artist.name %></h2>
        <%= image_tag @artist.image.url, size:'200x200' %>
      </div>
    </div>
  </div>
</div>